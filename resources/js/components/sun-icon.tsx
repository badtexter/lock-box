import type { LucideIcon } from 'lucide-react';
import { Sun } from 'lucide-react';

/**
 * Reusable Sun icon component used across the application for light theme toggle.
 * Change the icon here to update it everywhere.
 */
export const SunIcon: LucideIcon = Sun;
export const SunThemeIcon = SunIcon;
export default SunIcon;
